#books.transitions-enabled
	- @books.each do |book|
		.box.panel.panel-default
			= link_to ( image_tag book.image.url), book
			.panel-body
				%h2= link_to book.title, book
				%p.user
				Submitted by 
				= book.user.email